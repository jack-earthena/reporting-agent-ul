# Port (Railway sets PORT automatically)
PORT=3081

# Path to CSV data folder (default: ./data when not set)
# DATA_DIR=./data

# Rate limit: max report requests per IP per minute
RATE_LIMIT=20

# CSV cache TTL in milliseconds (default: 1 hour)
# CACHE_TTL_MS=3600000
